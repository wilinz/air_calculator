// ─── Light theme palette ──────────────────────────────────────────────────────
part of '../formula_editor_page.dart';

class _Pal {
  static const scaffold = Color(0xFFF7F8FA); // 页面底色
  static const appBar = Color(0xFFFFFFFF); // 顶栏底色
  static const previewBg = Color(0xFFFFFFFF); // 预览区底色
  static const rawBg = Color(0xFFF1F3F5); // LaTeX 原文条底色
  static const divider = Color(0xFFE3E5E8);
  static const formulaText = Color(0xFF1F2937); // 公式主色（深灰）
  static const fracBar = Color(0xFF374151); // 分数线、根号横线
  static const placeholder = Color(0xFFB0B6BD); // 空槽边框
  static const cursor = Color(0xFF1976D2); // 蓝色光标（与 Symbolab 一致）
  static const accent = Color(0xFFE53935); // 主操作红（确定按钮）
  static const operator = Color(0xFFE53935); // 运算符按钮文字色
  static const text = Color(0xFF1F2937);
  static const textMuted = Color(0xFF6B7280);
  static const textWeak = Color(0xFFB0B6BD);
  static const keyBg = Color(0xFFFFFFFF);
  static const keyBgOp = Color(0xFFFFFFFF);
  static const keyShadow = Color(0x14000000);
  static const tabIndicator = Color(0xFFE53935);
  static const error = Color(0xFFD32F2F);
  static const errorBg = Color(0xFFFDECEA);
}
